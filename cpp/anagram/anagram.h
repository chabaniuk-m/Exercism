#if !defined(ANAGRAM_H)
#define ANAGRAM_H

#include <string>
#include <vector>

namespace anagram {
    class anagram {
    private:
        std::vector<char> word;
    public:
        anagram(const std::string word)
    };
}  // namespace anagram

#endif // ANAGRAM_H